name: tekartik_build_node
description: Build node helper
version: 0.2.3
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  path: ">=1.9.1"
  node_preamble:
  process_run: ">=1.2.1+1"
  dev_build: ">=1.1.7+3"

dev_dependencies:
  test: ">=1.31.0"
  lints: ">=6.1.0"
  build_web_compilers: ">=4.4.19"
  build_runner: ">=2.15.0"
resolution: workspace
