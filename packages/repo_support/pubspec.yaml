name: repo_support
description: Repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ">=6.1.0"
  path: ">=1.9.1"
  pub_semver: ">=2.2.0"
  process_run: ">=1.2.1+1"
  test: ">=1.31.0"
  dev_build: ">=1.1.7+3"
  tekartik_build_node:
    path: ../build_node
resolution: workspace
